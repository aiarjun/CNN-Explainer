import torch
import random
import numpy as np
from scipy.ndimage.filters import gaussian_filter1d
import matplotlib.pyplot as plt
from PIL import Image

# fool train

# return grids/images with 4 cols

# main function that computes + passes it to return grids and gets the list of PILs and saves it

# params: number of images, target y(or random), and output dir, filename: label + to + label + random str.png