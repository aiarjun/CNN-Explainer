import torch
import random
import numpy as np
from scipy.ndimage.filters import gaussian_filter1d
import matplotlib.pyplot as plt
from PIL import Image

# fool train

# return grids/images with 4 cols

# main function that computes + passes it to return grids and gets the list of PILs and saves it

# params: number of images, and iters and optionally those blur/print_every args output dir, 
# filename: label + viz + label + random str.png