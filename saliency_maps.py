import torch
import random
import numpy as np
from scipy.ndimage.filters import gaussian_filter1d
import matplotlib.pyplot as plt
from PIL import Image

# compute saliency

# return grids/images with saliency maps

# main function that computes + passes it to return grids and gets the list of PILs and saves it

# params: number of images, and output dir, filename: label + random str.png